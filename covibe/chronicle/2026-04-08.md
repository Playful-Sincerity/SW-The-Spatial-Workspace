# Session — 2026-04-08 15:28
**Project:** CoVibe
**Context:** Building V1 — skill, rule, hook script for multiplayer Claude Code

## 15:28 — [Implementation]

**What:** Built all three core CoVibe V1 components: `/covibe` skill (SKILL.md), coordination rule (covibe-coordination.md), and auto-sync hook (covibe-sync.sh).
**Why:** Following the plan from last session. V1 is git-based coordination — no server, no Agent SDK. Sessions communicate through markdown files in a shared repo.
**Means:** The coordination layer is functional. Skill has 8 commands (start, status, claim, msg, read, done, end, orchestrate). Rule enforces identity/read-before-work/write-after-work. Hook auto-commits and pushes `.covibe/` changes + nudges on stale sessions.
**Branch:** N/A (files written to ~/.claude/)
**Files:**
- `~/.claude/skills/covibe/SKILL.md` — created
- `~/.claude/rules/covibe-coordination.md` — created
- `~/.claude/scripts/covibe-sync.sh` — created

Settings.json edit blocked by deny rule — Wisdom needs to add the Stop hook manually.

---

## 15:28 — [Decision]

**What:** Wisdom decided CoVibe should be a standalone public repo, not kept private under Spatial Workspace.
**Why:** It's a coordination pattern (skill + rule + hook + markdown), not proprietary IP. More valuable open — portfolio piece, Hearth contribution, community tool.
**Means:** Need to create `~/Playful Sincerity/PS Software/CoVibe/` as a standalone repo with the source-of-truth copies. Users install by copying to their `~/.claude/`. The installed copies in `~/.claude/` are deployments, not the source.

---

## 15:28 — [Decision]

**What:** Wisdom wants every skill built for CoVibe to be "covibe-labeled" — meaning the repo is the canonical home for CoVibe skills.
**Why:** CoVibe is a product, not just a personal tool. Skills should ship with the repo so anyone can install them.
**Means:** The public repo structure needs a `skills/` directory where each skill lives. The `/covibe` skill is the first, but future skills (review, orchestrate, etc.) should also live there.

---

## 15:43 — [Implementation]

**What:** Created standalone public CoVibe repo at `~/Playful Sincerity/PS Software/CoVibe/`. Pushed to Playful-Sincerity/covibe on GitHub.
**Why:** CoVibe is more valuable open than closed. Installable by anyone with Claude Code.
**Means:** Source of truth is now in the CoVibe repo. `~/.claude/` copies are deployments. Repo has README with install instructions, CLAUDE.md, and the three components (skill, rule, hook).
**Branch:** `main`
**Files:**
- `CoVibe/README.md` — install guide + usage docs
- `CoVibe/CLAUDE.md` — project overview + architecture
- `CoVibe/skills/covibe/SKILL.md` — source copy
- `CoVibe/rules/covibe-coordination.md` — source copy
- `CoVibe/scripts/covibe-sync.sh` — source copy

Verified: hook exits 0 when no session active. Hook runs correctly with mock active session. All three component files exist at install paths.

---

## 17:35 — [Implementation]

**What:** Built install.sh and ran full end-to-end dry run of the CoVibe flow.
**Why:** Need Frank to install in one command, and need to verify the whole cycle works before piloting.
**Means:** Install script works (tested). Full flow verified: start (identity + session file + config + active marker) → msg (message file created) → claim (job status + session updated) → sync hook (auto-commits each step). All clean exits, correct git history.
**Branch:** `main`
**Files:**
- `CoVibe/install.sh` — one-command installer
- `CoVibe/README.md` — updated with clone-and-run install
- `Spatial Workspace/covibe/CLAUDE.md` — added redirect to standalone repo

Pushed to GitHub. Frank can now: `git clone` → `./install.sh` → add one line to settings.json → go.

---

## 2026-04-10 05:51 — [Architecture]

**What:** Added plugin packaging and unified CoVibe's job board with multi-session decomposition format.
**Why:** Wisdom realized the phased session brief structure IS how CoVibe should organize work. And plugin distribution means one-line install instead of clone + manual setup.
**Means:** Two distribution methods now: (1) Claude Code plugin via `enabledPlugins: {"covibe@playful-sincerity": true}`, or (2) `git clone` + `./install.sh`. Orchestrate command now produces phased directory structure (`phase-N/subphase-X-TYPE-deps/`) where each job file is simultaneously a CoVibe job and a cold-startable session brief. This unifies multi-session decomposition (planning format) with CoVibe (coordination layer) into one system.
**Branch:** `main`
**Files:**
- `.claude-plugin/plugin.json` — plugin manifest
- `.claude-plugin/marketplace.json` — marketplace config for Playful-Sincerity
- `hooks/hooks.json` — Stop hook via `${CLAUDE_PLUGIN_ROOT}`
- `skills/covibe-coordination/SKILL.md` — model-invoked coordination guidance (replaces rule for plugin users)
- `skills/covibe/SKILL.md` — updated orchestrate command with phased structure
- `README.md` — plugin install instructions
- `install.sh` — updated to copy coordination skill

---

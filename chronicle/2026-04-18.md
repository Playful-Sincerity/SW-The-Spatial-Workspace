# Session — 2026-04-18 — Pick up Phase 3D layout

**Project:** Spatial Workspace v2
**Context:** Resuming from 2026-04-17 carryover. Two issues Wisdom flagged on reopen: (1) too much space between nodes, (2) a visible "invisible corner" getting pushed — the rectangular clamp boundary is showing.

---

## HH:MM — [Discovery]

**What:** Read CLAUDE.md, 2026-04-17 chronicle, build-queue, `createLayout()` lines 157–487. Identified root causes.
**Why:** Before touching code, get the full picture per the carryover's "don't stack more forces, revert and think" directive.
**Means:** The invisible-corner artifact is the hard clamp at `app.js:465-474` — `MAX_R = 600 + sqrt(nodes.length)*120` with a rectangular `if (d.x > MAX_R) d.x = MAX_R` per axis. Nodes that drift hit the box and stack at the wall. The spacing is driven by `linkPad=20` with depth multipliers up to 1.8x, `chargeBase=-60`, PLUS the outward-bias and bubble forces that both push nodes apart, PLUS subtree-weight sizing that scales root 1.9x for 3k leaves.
**Branch:** `main`
**Files:** read-only

---

## HH:MM — [Implementation]

**What:** Removed the rectangular position clamp (the "invisible corner"). Implemented pin-on-expand: carried-over nodes get `fx`/`fy` during sim so they don't move when a user expands a new folder. Only new children run through physics. Added a `pinStrength` slider (0–1, default 1.0 = hard pin). Also zeroed charge for pinned nodes — otherwise every pinned node within 400px one-way-repels the new children, shoving new clusters far from frozen parents.
**Why:** Wisdom's insight: *"you don't even need to change the previous, just let the new one pop in... the ecosystem is the navigation history, there should only ever be one shape."* Pin-on-expand is literally this — spatial arrangement becomes memory, not rendered output. Fixed a latent bug: the click-to-expand handler was forcing `isResettle: true`, which wiped `_carried` status on every node; changed to default `false` so pinning actually activates on expand.
**Means:** Four code touches. `SW_PHYSICS.pinStrength` default 1.0. `updateTree` flags `d._carried = true` when copying from `_prevPositions`. `createLayout.run()` sets `fx/fy` on carried nodes when `PIN_HARD`, releases after `resolveOverlaps`. `chargeStrength` returns 0 for pinned nodes when hard-pinned. Physics panel slider shows "hard"/"off"/numeric based on value.
**Branch:** `main`
**Files:** `templates/v2/app.js` (lines 171-178, 282-302, 308-319, 424-434, 518-525, 1140-1156, 1376, 2067), `~/Wisdom Personal/people/wisdom-profile/intellectual-signature.md` (shape-as-memory insight)

---

## HH:MM — [Pivot]

**What:** Cloned canvas into two parallel prototypes. Pinned Expanse = current behavior (clicked folder is rigid, children squeeze into available space). Dynamic Expanse = to-be-built (clicked folder nudges outward just enough to give its new children a clean bubble). Wrote session brief for a fresh-conversation Claude to build dynamic-expanse.
**Why:** Wisdom after three rounds of membrane debugging: *"we've got two options there... maybe we spawn a parallel version of this whole thing, just clone the whole thing and then do the two different options."* Classic preference-testing-by-building — feel both in use before choosing. Wisdom's labels: "pinned expanse" vs "dynamic expanse."
**Means:** `cp -r templates/v2 templates/v2-dynamic` for initial byte-clone. Added `--template` CLI arg to the generator (it already had `--output`). Built both: `~/ecosystem-canvas-pinned.html` from `v2/`, `~/ecosystem-canvas-dynamic.html` from `v2-dynamic/` (identical for now). Session brief at `sessions/v3/dynamic-expanse-prototype.md` has cold-start context, the core diff to make, file regions to touch, test plan, and an explicit invariant list.
**Branch:** `main`
**Files:** `templates/v2-dynamic/` (clone), `generator/generate-ecosystem.py` (template arg), `sessions/v3/dynamic-expanse-prototype.md` (brief), two new output HTMLs.

---

## HH:MM — [Implementation]

**What:** Fixed the membrane-crossing bug in pinned-expanse. Bubble force now uses direct position correction when `PIN_HARD`, always (not just asymmetric pairs).
**Why:** Prior fix had a flaw — treated "kid is carried" as "subtree is rigid," skipped both-carried pairs. But a carried parent can have mobile new children. The Companion (pinned, no mobile desc.) vs HHA (pinned, *with* mobile new children) hit the skip path, lost membrane enforcement, nodes overlapped.
**Means:** One branch simplified. If `PIN_HARD`: always use `applySubtreePosition` (respects `fx`/`fy`, skips pinned descendants, moves only mobile ones by half-overlap per tick). Else: original velocity push.
**Branch:** `main`
**Files:** `templates/v2/app.js` (subtreeBubble sibling-pair block)

---

# Session — 2026-04-18 13:22 — Dynamic Expanse prototype
**Project:** Spatial Workspace v2 (dynamic-expanse variant)
**Context:** Fresh conversation per `sessions/v3/dynamic-expanse-prototype.md`. Diverge `templates/v2-dynamic/app.js` from `v2/` so the clicked folder is the one mobile node — nudges outward to give new children a clean bubble while all other previously-placed nodes stay literally fixed.

---

## HH:MM — [Coordination]

**What:** Wisdom redirected the original (pinned-expanse) conversation to also implement dynamic-expanse after telling it "let's do this one." Both conversations touched `templates/v2-dynamic/app.js` independently. This entry is written from the original (pinned-expanse) conversation to announce its implementation so the 13:22 conversation can decide whether to continue, diff, or drop.
**Why:** Unclear-handoff-between-sessions pattern. Session brief says "start a fresh conversation"; Wisdom's next message read ambiguous.
**Branch:** `main`
**Files:** `templates/v2-dynamic/app.js`, `~/ecosystem-canvas-dynamic.html` (regenerated)

---

## HH:MM — [Implementation]

**What:** Built dynamic-expanse from the original conversation. Click-to-expand now threads a `justExpanded` path through `updateTree` → `createLayout`. The clicked node gets a medium soft-spring back toward its prior position instead of hard `fx`/`fy`. Bubble force gives it velocity-drift against pinned siblings so it nudges outward just enough to make room, held back by the spring.
**Why:** Wisdom wanted to feel both semantics side-by-side before choosing.
**Means:** Five-touch diff in `templates/v2-dynamic/app.js`. (1) `updateTree` signature accepts `justExpanded`. (2) Descendants loop sets `d._justExpanded` and `d._carried = !d._justExpanded`. (3) Click handler passes `justExpanded: clickedPath` on expand, nothing on collapse. (4) Pin setup tracks `justExpandedNode` separately — stores `_pinX`/`_pinY` but not `fx`/`fy`. (5) `pinSpring` always applies `kJust=0.35` pull on just-expanded node. (6) Bubble adds a branch: if either side is `_justExpanded`, velocity push on that side (drift), skip pinned side.
**Branch:** `main`
**Files:** `templates/v2-dynamic/app.js` (all five regions), `~/ecosystem-canvas-dynamic.html` regenerated.

---

## 13:27 — [Coordination]

**What:** 13:22 conversation acknowledging the parallel-conversation handoff. `templates/v2-dynamic/` is owned by the original conversation; this conversation takes `templates/v2-dynamic-alt/` as its own sandbox (byte-identical clone of `templates/v2-dynamic/` at handoff, so it starts with the working dynamic-expanse implementation). Output goes to `~/ecosystem-canvas-dynamic-alt.html`. Any future tuning of the dynamic-expanse semantics from this thread happens in `-alt/` only, never in `v2-dynamic/`.
**Why:** Wisdom: *"just make sure everything you're editing in there is yours and that we're organized well."* Two conversations writing the same file is exactly the kind of parallel-work collision that produces lost edits. Clean ownership per folder resolves it.
**Branch:** `main`
**Files:** `templates/v2-dynamic-alt/` (clone), `~/ecosystem-canvas-dynamic-alt.html` (generated).

---

## 13:30 — [Insight]

**What:** Wisdom sent a voice-note idea while away from the computer — a phototropism framing for what the layout is optimizing. Eyes = light, files = tree, buttons = leaves, lines = structural mass. The tree wants to maximize leaf surface area facing the light + individual leaf size, while minimizing structural mass and overlap.
**Why:** Offered tentatively ("whether or not that's essentially what we're modeling is another story. I just wanted to capture that idea here"). Captured because it names an *objective function* where the current layout engine only has forces. Could become a concrete criterion for pinned-vs-dynamic eye-tests: "which one catches more light with less mass?"
**Means:** Three-file capture. (1) Raw speech preserved at `knowledge/sources/wisdom-speech/2026-04-18-phototropism-metaphor.md`. (2) Full idea write-up at `ideas/2026-04-18-phototropism-layout-philosophy.md` with the four measurable quantities, open questions, and notes on how it could inform future layout engines. (3) Short pointer in `ideas/build-queue.md`.
**Branch:** `main`
**Files:** three new files listed above; none implemented.

---

## 13:45 — [Implementation]

**What:** Fixed runaway drift on the just-expanded node in `v2-dynamic-alt`. Screenshot from Wisdom showed the clicked "Playful Sincerity" subtree ejected to the opposite corner of the canvas, leaving a vast empty middle between it and an earlier-expanded subtree.
**Why:** Three forces were pumping velocity outward on C (just-expanded node) simultaneously — bubble force velocity push, charge repulsion from C's own new children, and outwardBias against the grandparent vector. The `k=0.35` spring couldn't catch up: velocity accumulated across ~200 ticks before decay + spring could neutralize it.
**Means:** Three edits to `templates/v2-dynamic-alt/app.js`. (1) Bubble force's `iJust || jJust` branch: replaced velocity push with direct position correction (matches the pinned-expanse membrane fix pattern — resolves overlap in one tick, then stops). (2) `chargeStrength`: returns 0 for `d._justExpanded` — C acts as an anchor for its new children like a pinned node does, but without needing fx/fy. (3) `outwardBias`: `continue` on `d._justExpanded` — position is driven by overlap resolution only, no radial geometric pressure. Net: C stays at prior position unless overlap exists, then moves by minimum-displacement-to-resolve — no accumulation.
**Branch:** `main`
**Files:** `templates/v2-dynamic-alt/app.js` (three regions: ~L320, ~L335, ~L440), `~/ecosystem-canvas-dynamic-alt.html` regenerated.

---

## 14:05 — [Implementation]

**What:** Still drifting too far after the first three-edit fix. Screenshot showed C at the far corner. Replaced the soft-spring-on-C approach with a velocity-freeze: zero C's velocity every tick after all forces run.
**Why:** Charge from C's own new children, gentleCenter, and link force still inject velocity into C every tick. The `k=0.35` spring couldn't match that — over ~200+ ticks velocity accumulated into major drift. The bubble force (now position-correction, not velocity) doesn't touch velocity, so freezing velocity doesn't block overlap resolution.
**Means:** Two edits to `templates/v2-dynamic-alt/app.js`. (1) Removed the `justExpandedNode` soft-spring block from `pinSpring`. (2) Added `freezeJustExpanded` force registered LAST in both sim chains (after `center`) that sets `justExpandedNode.vx = 0; vy = 0`. Net semantics now: C cannot move via any velocity-based force; the only thing that can move C is the bubble force's direct position correction (which respects fx/fy and modifies position directly). So C stays at prior position *unless* the bubble force detects overlap with a pinned sibling, then C moves by exactly minimum displacement to resolve.
**Branch:** `main`
**Files:** `templates/v2-dynamic-alt/app.js` (~L478, ~L489, ~L545, ~L574), `~/ecosystem-canvas-dynamic-alt.html` regenerated.

---

## 2026-04-19 / 02:00 — [Pivot / Reset Declared]

**What:** After ~17 hours of iterative popout + physics tuning attempts (bounded bubble push, velocity freeze, wedge layout, pop-outward, walk-outward popout, BFS-popout during initial render), every approach either reintroduced drift, produced sunburst-style geometry Wisdom explicitly rejected, or left the fundamental "top-level branches spread too far" problem unsolved. Called a hard reset: reverted `templates/v2-dynamic-alt/app.js` to a byte-identical copy of `templates/v2/app.js` + the test marker. All today's physics edits gone.
**Why:** Wisdom: *"cool can you make a document for us to do a reset?"* The accumulating-fixes pattern was poisoning context — each new edit fought the previous one. Two-Correction Rule long since tripped. Real honest answer: force-directed physics mathematically spreads at 3800-node ecosystem scale; no tuning will converge. The substrate has to change.
**Means:** `cp templates/v2/app.js templates/v2-dynamic-alt/app.js` + re-added test marker. Wrote `sessions/v3/2026-04-19-reset-after-dynamic-alt.md` — a self-contained brief for a fresh next-session Claude, with: (a) current state, (b) the seven attempts tried today and why each failed, (c) Wisdom's load-bearing diagnostic ("physics is global, we want local"), (d) Wisdom's desired aesthetic extracted from 17 hours of feedback, (e) the two genuine paths forward — nested circle packing (d3.pack) vs fractal tree (L-system) — with tradeoffs, (f) gotchas (watch-server template flag, test marker, why not to tune physics further), (g) how to start the next session (read the brief + SPEC + think-deep synthesis, commit to ONE path, fresh sandbox `v3-packing/` or `v3-tree/`).
**Branch:** `main`
**Files:** `templates/v2-dynamic-alt/app.js` (full reset from v2/ + test marker), `sessions/v3/2026-04-19-reset-after-dynamic-alt.md` (the reset brief), `generator/watch-server.py` (earlier today — added `--template` and `--output` flags).

**End-of-day state:** dynamic-alt.html serves the same as v2/ right now (with test marker). No new substrate built. Clean slate for tomorrow. Artifacts that survive today: SPEC-CLICK-TO-GROW.md, `research/think-deep/2026-04-18-click-to-grow-substrate.md`, `ideas/2026-04-18-phototropism-layout-philosophy.md`, `knowledge/sources/wisdom-speech/2026-04-18-phototropism-metaphor.md`, `ideas/build-queue.md` entries (drag-and-drop, bulk-expand), this chronicle.

---

## 18:10 — [Research]

**What:** Full Think Deep session on "what's the simplest click-to-grow mechanism that satisfies every criterion?" triggered by Wisdom's step-back after the third velocity-freeze tuning pass failed the eye-test. Includes: deep-cause diagnosis of physics drift, dream agent (seven scenarios), research-algorithms agent (narrow survey of incremental placement), reframe from layout engine to position-assignment service, and a shift of primary algorithm from phyllotaxis to proportional angular sector allocation ("wedge/balloon" layout).
**Why:** Wisdom: *"what is making this go all the way out? What is the simplest possible math and logic behind this?"* The chronicle from earlier today shows three force-field patches that each reduced drift without solving it. The deep cause (physics has no native concept of "minimum disturbance") is structural — we'd been trying to bend a differential-equation integrator into doing placement's job.
**Means:** Dream agent generated seven walked-through interaction scenarios and a pseudocode sketch. Research agent surveyed phyllotaxis, MTV, fCoSE, yFiles Partial, Graphviz neato pin, d3.pack internals, and the Beck 2016 dynamic-graph-drawing survey. Key findings: (1) click-to-grow = position-assignment service, not layout engine; (2) wedge allocation handles non-uniform weights natively and prevents sibling overlap by construction — it's what mxGraph/draw.io, yFiles BalloonLayouter, and ELK Radial all ship; (3) fCoSE `fixedNodeConstraint` is the polish-pass fallback. Primary mechanism spec'd with seven constants and no physics. Implementation estimate: <200 LOC for the wedge placement kernel.
**Branch:** `main`
**Files:**
- `SPEC-CLICK-TO-GROW.md` (new, project root) — the actionable spec
- `research/think-deep/2026-04-18-click-to-grow-substrate.md` — the think-deep synthesis
- `research/think-deep/2026-04-18-click-to-grow-substrate/agents/dream-scenarios.md` — seven dream walks
- `research/think-deep/2026-04-18-click-to-grow-substrate/agents/research-algorithms.md` — narrow algorithm survey

---

## 18:15 — [Connection]

**What:** During the think-deep Wisdom surfaced a positioning line offhand: "It's an operable canvas visualization of a repository. That's very useful. That's something GitHub should adopt at some point." Noting this for product narrative (separate from the technical spec).
**Why:** GitHub Next's `repo-visualizer` produces static SVGs for READMEs. Spatial Workspace is the *interactive, persistent, navigable* version — adds the spatial-memory layer that static visualizations can't. This positioning cleanly differentiates from every existing tool in the space.
**Means:** Captured in the think-deep synthesis under Open Threads #6 for later extraction into marketing/pitch material. Not a technical change.
**Branch:** `main`
**Files:** `research/think-deep/2026-04-18-click-to-grow-substrate.md` (Open Threads section)

---

## 19:05 — [Pivot]

**What:** Abandoned the bag-detection Stage 1 plan mid-build. Wisdom rejected the bag/grid split: *"we had it... it's really a bummer we had it, it was just things were just getting pushed too far away for some reason that was the main thing, the bubbles were working."* The pivot: don't add bag rendering, don't introduce new algorithms. Just disable the inter-subtree separating force in `v2-dynamic-alt` and see what happens.
**Why:** His diagnosis reframed the whole day's work. The bubble-force push was treating "subtrees about to overlap" as a problem that needed active separation. But the separation itself was the "too far out" bug. If we stop separating, individual-node collision still prevents button overlap, and subtree membranes deform at the edges instead of shoving each other apart — soap-film behavior. Much closer to his aesthetic ("organic > grid") and his stated mental model ("bubbles were working").
**Means:** One edit to `templates/v2-dynamic-alt/app.js` — disabled the sibling-subtree push block inside `subtreeBubble`. Kept the territory map T computation (free, reusable if we add capped containment later). Also captured the drag-and-drop-buttons idea Wisdom surfaced in overwhelm as a build-queue entry.
**Branch:** `main`
**Files:** `templates/v2-dynamic-alt/app.js` (~L425-476 sibling-push block), `ideas/build-queue.md` (new entry), `~/ecosystem-canvas-dynamic-alt.html` regenerated.

---

## 19:15 — [Implementation]

**What:** Re-enabled the sibling-subtree push, but bounded. The no-push version was too permissive — once you expand anything new, bubbles must give a little, which fully-disabled push couldn't deliver. Wisdom: *"it's impossible to make them pin, they've got to go out, just a matter of how to make sure it's tuned well so it doesn't go out to the stratosphere."*
**Why:** Needed push, but with two constraints the earlier versions didn't have: (1) tolerance zone — small overlaps allowed, soap-film deform at boundaries, (2) per-tick cap on displacement so no accumulation can drift to stratosphere. `OVERLAP_TOLERANCE=15px` (bubbles may overlap by 15px with no push), `MAX_PUSH_PER_TICK=1px` (hard ceiling per-pair per-tick).
**Means:** Replaced the no-op block with a simple bounded push — `step = min(effectiveOverlap, MAX_PUSH_PER_TICK) * 0.5` using `applySubtreePosition` so pinned nodes stay pinned and only mobile descendants move. Removed all prior variants (velocity push, asymmetric pair handling, position correction halves) — one clean loop, one rule. Kept `freezeJustExpanded` and the other today's fixes as they complement this cleanly.
**Branch:** `main`
**Files:** `templates/v2-dynamic-alt/app.js` (subtreeBubble push block rewritten, ~25 LOC), `~/ecosystem-canvas-dynamic-alt.html` regenerated.

---

## 2026-04-19 / 00:30 — [Pivot / Reverted]

**What:** Built the wedge algorithm (replaced all physics in `templates/v2-dynamic-alt/app.js` with deterministic angular-sector placement — ~60 LOC function, deleted ~13.8KB of forces/sim). Wisdom reverted on first eye-test: *"oh I see, yeah no let's revert back, because this is just the wedge, we don't want the wedge, we want more like the leaves, we want more like a tree."*
**Why:** Wedge produces a geometric sunburst feel — each node lives inside a fixed angular sector, children subdivide the sector, everything radiates from origin. Deterministic and physics-free, yes, but also visually *regular* — the exact "too regular, sterile" aesthetic Wisdom has been rejecting since the start of this thread. His mental model is "tree with leaves" — organic, bubble-like, branches that feel grown rather than assigned.
**Means:** Reverted v2-dynamic-alt by copying `templates/v2-dynamic/app.js` over it (parallel conversation's physics version, untouched today — clean known-good baseline). All of today's experimentation in v2-dynamic-alt discarded: bounded bubble push, charge/linkPad/K tuning, gentleCenter strengthening, wedge rewrite. Sandbox reset.
**Branch:** `main`
**Files:** `templates/v2-dynamic-alt/app.js` (full file reset from `templates/v2-dynamic/app.js`), `~/ecosystem-canvas-dynamic-alt.html` regenerated.

**Lesson for the next iteration:** The criteria tension surfaces clearly now — Wisdom wants *organic* (physics-directed, emergent, bubble-like) AND *stable* (no drift, no spread-to-stratosphere). Those aren't opposed in principle but they're opposed in the mechanisms we've tried. The SPEC-CLICK-TO-GROW.md wedge approach solves stability at the cost of organic feel. The physics approach has the organic feel but can't be pinned. The genuine third path — which yesterday's research flagged as Option 1 (d3.pack nested circle packing) — hasn't been tried. Circle packing produces "leaves inside leaves" organic visuals (that's what FoamTree and GitHub Next's repo-visualizer look like) AND inherits d3.pack's deterministic placement. Worth proposing as the next substrate attempt if physics-with-tuning fails again.

---
